/** Visible product name in UI copy, headers, and page metadata. */
export const DIVLAB_BRAND_NAME = "DivLab";

/** Fallback display label for members without a public name. */
export const DIVLAB_MEMBER_LABEL = `${DIVLAB_BRAND_NAME}-medlem`;
